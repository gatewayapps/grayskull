import { IRequestContext } from '../../foundation/context/prepareContext'

export async function listAuthorizedUsersActivity(
	clientId: string,
	limit = 100,
	offset = 0,
	context: IRequestContext
) {}
