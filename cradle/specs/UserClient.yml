UserClient:
  meta:
    topLevel: true
  properties:
    userAccountId: integer unique('userClient')
    client_id: uniqueidentifier unique('userClient')
    createdBy: integer
    createdAt: datetime default(NOW)
    revoked: boolean default(false)
    revokedBy: integer?
    RevokedAt: datetime?
