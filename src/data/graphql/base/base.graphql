scalar Date
type Query {
  securityConfiguration: SecurityConfiguration!
  serverConfiguration: ServerConfiguration!
}



enum Role {
  None
  User
  Admin
}

directive @auth(
  requires: Role
) on OBJECT | FIELD_DEFINITION

directive @authOrSelf(
  requires: Role
) on OBJECT | FIELD_DEFINITION
