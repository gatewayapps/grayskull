type UserProfile {
  userAccountId: String!
  firstName: String!
  lastName: String!
  displayName: String
  gender: String
  birthday: Date
  profileImageUrl: String
  permissions: Int!
  lastPasswordChange: Date
  emailAddress: String
  otpEnabled: Boolean
}

type Query {
  me: UserProfile
}

